version: "3.9"
services:
  web:
    image: my-bun-app:latest
    ports:
      - "80:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # volumes: (optional) - don't mount source in production
